{
  "scripts": {
    "watch:sass": "sass components/accordion.scss components/accordion.css -w",
    "devserver": "live-server",
    "start": "npm-run-all --parallel devserver watch:sass"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.6.2",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.6.2"
  }
}
